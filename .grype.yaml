# No suppressions are active. Wolfi shipped the CVE-2026-85091 zlib fix as
# 1.3.2.1_rc20260601-r0, which the current wolfi-base digest carries, so the
# entries that used to sit here are obsolete. Keep the file so image-scan.yml
# can go on passing --config, and add entries only for a CVE that is genuinely
# unfixable at the pinned digest.
ignore: []
